---
description: Fix a reproduced suspect and prove the fix.
argument-hint: "<incident id, e.g. INC-001>"
---

Switch to the 🩹 Culprit Fixer mode, then follow the mode's custom instructions exactly.

The incident id is: $ARGUMENTS (default INC-001 if not given).
