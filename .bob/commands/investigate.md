---
description: Test every suspect of an incident in parallel and record verdicts.
argument-hint: "<incident id, e.g. INC-001>"
---

Switch to the 🔎 Culprit Investigator mode, then:

1. Run `python -m culprit open $ARGUMENTS` (default: INC-001 if no argument given).
2. Follow the Culprit Investigator mode's custom instructions exactly.

If no incident id was provided, use INC-001.
